import Button from "@/components/Button";

type RephraserProps = {};

const Rephraser = (props: RephraserProps) => {
  return (
    <div className="mt-10">
      Rephrase
      <input type="text" />
    </div>
  );
};

export default Rephraser;
